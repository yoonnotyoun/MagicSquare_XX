# 세션 3 인프라 구축 — 프롬프트

**프로젝트:** MagicSquare_XX  
**용도:** Harness·Rule·Command 구축 세션 replay용 transcript  
**관련 보고서:** `Report/04_세션3_인프라_구축_MagicSquare_XX.md`

---

## 대화 transcript

### Turn 1 — 사용자

```
MagicSquare_XX 세션 3용 최소 Harness만 만들어줘. 본문은 비워.
- pyproject.toml : pytest 만 설정
- src/__init__.py : 빈 파일
- src/validate_lines.py : 함수 시그니처만 (def validate_lines(grid): ...)
- tests/__init__.py : 빈 파일
- tests/test_validate_lines.py : 빈 파일 (import 줄만)
구현·테스트 본문은 아직 쓰지 마. 골격만.
```

### Turn 1 — 에이전트

- `pyproject.toml`: `[tool.pytest.ini_options]` — `testpaths`, `pythonpath`
- `src/validate_lines.py`: `def validate_lines(grid): ...`
- `tests/test_validate_lines.py`: `from src.validate_lines import validate_lines`
- `pytest --collect-only` → `no tests collected` (정상)

---

### Turn 2 — 사용자

```
루트에 .cursorrules 초안을 만들어줘. 40~60줄. 다른 파일은 만들지 마.
도메인: 4×4, 빈칸 0, 1~16, 마법상수 34, 10선(R1~R4·C1~C4·D1·D2)
API: validate_lines(grid) -> {status: pass|fail|incomplete, failed_lines:[...]}
ECB 패턴으로 작성
TDD: RED→GREEN→REFACTOR, assert 완화·skip·xfail 금지, RED은 tests/만 수정
AI: 한국어, TDD 시 첫 줄 Phase 선언, git commit은 사용자 요청 시만
```

### Turn 2 — 에이전트

- `.cursorrules` 56줄 생성
- 섹션: 도메인, 공개 API, ECB, Mom Test 게이트, TDD, AI 작업 규칙
- 다른 파일 미생성

---

### Turn 3 — 사용자

```
하네스를 커서 룰을 기반으로 갱신해줘.
```

### Turn 3 — 에이전트

- `src/entity/__init__.py`, `src/entity/constants.py` 추가  
  (`GRID_SIZE`, `MAGIC_CONSTANT`, `EMPTY_CELL`, `MIN_CELL`, `MAX_CELL`, `LINE_IDS`)
- `src/validate_lines.py`: `list[list[int]] -> dict` + Returns docstring
- tests import 유지, 구현·테스트 본문 없음

---

### Turn 4 — 사용자 (Ask mode)

```
@.cursorrules
빠진 규칙을 표로만 알려줘. 파일 수정 금지.
체크: 10선 정의, 마법상수 34 SSOT, incomplete 처리, RED 금지사항.
```

### Turn 4 — 에이전트

- 파일 미변경
- 빠진 규칙 표 제공 (예: grid 인덱스 매핑, SSOT 미선언, incomplete 우선순위, RED `src/` 전체 금지 세부 등)

---

### Turn 5 — 사용자

```
.cursor/commands/tdd-red.md 를 만들어줘. validate_lines RED 단계 전용.
포함: Phase 선언, AAA 절차, pytest 예시, 보고 형식,
      금지(src/ 수정·assert 완화). 다른 파일은 만들지 마.
```

### Turn 5 — 에이전트

- `.cursor/commands/tdd-red.md` 생성
- Phase 선언, AAA, pytest 예시(`test_d1_sum_not_34_returns_fail`), RED 보고 형식, 금지 표

---

### Turn 6 — 사용자

```
.cursor/commands/export-session.md 를 만들어줘.
이번 대화를 정리해 /Report 폴더에는 보고서 생성, /Prompt 폴더에는 transcript export.
번호 규칙: Report/와 Prompt/ 아래에 있는 기존 NN_ 파일을 확인하고 가장 큰 번호 +1을 다음 번호로 씀. 기본 형식은 2자리.
금지: 기존 파일 덮어쓰기, report와 prompt 중 하나만 생성하기, 다른 폴더에 있는 파일 수정하기
```

### Turn 6 — 에이전트

- `.cursor/commands/export-session.md` 생성
- `Report/04_세션3_인프라_구축_MagicSquare_XX.md` 생성
- `Prompt/04_세션3_인프라_구축_프롬프트.md` 생성 (본 파일)

---

## 재현 프롬프트 (요약)

```
MagicSquare_XX 세션 3 인프라를 순서대로 구축해줘:

1) 최소 Harness (pytest, validate_lines 시그니처, tests import만)
2) .cursorrules 40~60줄 (도메인·API·ECB·TDD·AI)
3) Harness ECB 갱신 (entity/constants.py, Control docstring)
4) .cursor/commands/tdd-red.md (validate_lines RED)
5) .cursor/commands/export-session.md + Report/Prompt 04번 쌍 export

구현·GREEN·테스트 본문은 아직 쓰지 마.
```

---

## 산출물 매핑

| Turn | 요청 | 결과 파일 |
|------|------|-----------|
| 1 | Harness 골격 | `pyproject.toml`, `src/**`, `tests/**` |
| 2 | Rule | `.cursorrules` |
| 3 | Harness 갱신 | `src/entity/constants.py`, `src/validate_lines.py` |
| 4 | Rule 점검 | (문서만, 파일 없음) |
| 5 | RED Command | `.cursor/commands/tdd-red.md` |
| 6 | Export | `.cursor/commands/export-session.md`, `Report/04_*`, `Prompt/04_*` |

---

## 다음 단계

- [ ] `/tdd-red` — Test Loop 사이클 1 RED (SC-02 대각선)
- [ ] `.cursorrules` SSOT·10선 좌표·incomplete·RED 세부 보완
